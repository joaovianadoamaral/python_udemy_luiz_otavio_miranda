# qualquer coisa(comentario em python)
# shift + alt + seta copia para baixo e para cima
# shift + alt + seta copia para baixo e para cima
# shift + alt + seta copia para baixo e para cima# shift + alt + seta copia para baixo e para cima
# use shortcuts pdf windows. (principais atalhos)

# configure seu .json do jeito certo

# comando pwd , mostra a pasta
# comando ls, mostra os itens da pasta
# não criar o hábito do autosave, controle quando salvar

# comando para executar um código no terminal: python nome_arquivo.py

# extensão python da microsoft. playzinho no topo
# 

# extensão code Runner
# use o .json para rodar no terminal. por padrão output

#cls ou clear para limpar
#cls não funciona em linux e macos

# dois comandos usaremos um ;(prompt) ou &&(powershell) depende 

# extensão Material Icon theme
# extensão Om Theme (A Darker Dracula Theme)

# é possivel copiar as configurações para outros sistesmas operacionais
# é preciso de modificar as linhas 'apagadas'
